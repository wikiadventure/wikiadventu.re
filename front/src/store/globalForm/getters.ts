import { GetterTree } from 'vuex';
import { StateInterface } from '../index';
import { GlobalForm } from './state';

const getters: GetterTree<GlobalForm, StateInterface> = {
  
};

export default getters;
